package ru.makotomc.boundingbox;

import net.fabricmc.api.ModInitializer;

public class BoundingBox implements ModInitializer {
    @Override
    public void onInitialize() {
//        PlayerEnterBoxCallback.EVENT.register((event) -> {
//            if (event.player.isCreative()) {
//                return true;
//            }
//            Box box = new Box(-10,-70,0,10,270,1);
//            if(event.isEntering(box)){
//                event.player.sendMessage(Text.literal("entering box"), true);
//                return false;
//            }
//            return true;
//        });
    }
}
